## A server that dumps all incoming requests in to a file

Dump locaiton output dir is specified in appsettings.json

`/your-path` - Saves dumps under your-path dir and dumps only the incoming request body

`/a/your-path` - Saves dumps under your-path dir but dumps the entire request